## 基本命令
```shell
git branch
git branch -a 
git branch -d branch_name					//删除本地分支
git push prigin --delete branch_name		//删除远程分支
git checkout -b dev                         //本地新建分支
git push origin dev:dev                     //推送远程仓库
```